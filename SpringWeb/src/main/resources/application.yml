spring:
  datasource:

    url: jdbc:mysql://localhost:3306/springweb?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: seongwon1290!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update

  jackson:
      property-naming-strategy: SNAKE_CASE

server:
    port: 9089